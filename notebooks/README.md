# notebooks/

Exploratory Jupyter notebooks for morphology / spectroscopy experiments.

Production lab runs should use `scripts/batch_morphology_analysis.py` (see `docs/guides/lab-workflow.md`). When a notebook becomes a stable procedure, extract it into the package + a documented script.
