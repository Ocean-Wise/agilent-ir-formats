# outputs/

Analysis products land here by default (e.g. `simplified_particle_results.csv`).

- Contents are **gitignored** — archive important results to lab storage, not git.
- Override the path with `--output` on the batch CLI if you need a campaign-specific name.

See [docs/reference/inputs-and-outputs.md](../docs/reference/inputs-and-outputs.md).
