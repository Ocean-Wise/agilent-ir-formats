# Legacy scripts

One-off patch, check, and timing scripts kept for history. **Not part of the lab SOP.**

Prefer `scripts/batch_morphology_analysis.py` and `scripts/analyze_results.py` for day-to-day work. If you revive something here, move a cleaned version into `scripts/` (or the package) and document it under `docs/guides/`.
