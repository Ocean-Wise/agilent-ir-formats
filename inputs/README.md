# inputs/

Drop **Agilent FTIR project folders** here for analysis.

- Recursive search finds all `*.dmt` files under this directory.
- Keep each project’s companion files together with its `.dmt`.
- Contents are **gitignored** — never commit session scans.

See [docs/guides/lab-workflow.md](../docs/guides/lab-workflow.md) and [docs/reference/inputs-and-outputs.md](../docs/reference/inputs-and-outputs.md).
